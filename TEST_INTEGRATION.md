# tmapi.top Integration Testing Guide

## Проверка интеграции с 1688 API

Этот документ описывает как протестировать реальную интеграцию с tmapi.top.

---

## ✅ Что реализовано:

✅ **Нет mock данных** - используются реальные API вызовы к tmapi.top  
✅ **Error Handling** - если API недоступна, приложение всё равно работает  
✅ **Type-safe** - полная поддержка TypeScript  
✅ **Validation** - проверка всех параметров  

---

## 🧪 Тестирование

### Шаг 1: Получить TMAPI_TOKEN

1. Зайдите на https://tmapi.top
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в **API Keys** или **API管理**
4. Скопируйте ваш **API Token**

### Шаг 2: Добавить токен в .env

```bash
# .env file
TMAPI_TOKEN=your_token_here
```

### Шаг 3: Перезагрузить приложение

```bash
npm run dev
```

---

## 🔍 Тестовые запросы

### 1️⃣ Тест поиска товаров

**Endpoint:** `POST /api/alibaba/search`

**Тестовый запрос:**
```bash
curl -X POST http://localhost:8080/api/alibaba/search \
  -H "Content-Type: application/json" \
  -d '{
    "keyword": "wireless earbuds",
    "pageNo": 1,
    "pageSize": 10,
    "sortBy": "price_asc"
  }'
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "data": [
    {
      "id": "627234567",
      "name": "Product name",
      "price": 8.50,
      "originalPrice": 12.99,
      "unit": "piece",
      "images": ["https://img.alicdn.com/..."],
      "seller": {
        "id": "b2b1234567",
        "name": "Supplier Name",
        "rating": 4.8
      },
      "minOrder": 1,
      "logistics": {
        "deliveryDays": 15,
        "shippingCost": 5
      }
    }
  ],
  "total": 1234,
  "pageNo": 1,
  "pageSize": 10
}
```

**Возможные ошибки:**
- `"TMAPI_TOKEN is not set"` → Добавьте токен в `.env`
- `"tmapi.top API error"` → Проверьте ваш токен
- Пустой массив `data: []` → Товар с таким названием не найден

---

### 2️⃣ Тест получения информации о товаре

**Endpoint:** `GET /api/alibaba/product/:productId`

**Тестовый запрос:**
```bash
# Сначала найдите productId из результатов поиска выше
curl http://localhost:8080/api/alibaba/product/627234567
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "data": {
    "id": "627234567",
    "name": "Wireless Earbuds Pro",
    "price": 8.50,
    "originalPrice": 12.99,
    "unit": "piece",
    "images": [
      "https://img.alicdn.com/image1.jpg",
      "https://img.alicdn.com/image2.jpg"
    ],
    "seller": {
      "id": "b2b1234567",
      "name": "Supplier",
      "rating": 4.8
    },
    "minOrder": 1,
    "logistics": {
      "deliveryDays": 15,
      "shippingCost": 5
    },
    "description": "High-quality wireless earbuds...",
    "specifications": {
      "category": "Electronics",
      "weight": "0.05",
      "size": "5x5x5cm",
      "material": "Plastic",
      "warranty": "12 months"
    }
  }
}
```

---

### 3️⃣ Тест расчета доставки

**Endpoint:** `POST /api/alibaba/shipping-estimate`

**Тестовый запрос:**
```bash
curl -X POST http://localhost:8080/api/alibaba/shipping-estimate \
  -H "Content-Type: application/json" \
  -d '{
    "productId": "627234567",
    "quantity": 100,
    "destination": "US"
  }'
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "data": {
    "productId": "627234567",
    "quantity": 100,
    "destination": "US",
    "shippingCost": 250.50,
    "currency": "USD",
    "estimatedDelivery": 20,
    "details": {
      "baseCost": 50,
      "perKgCost": 2,
      "totalWeight": "5.00kg",
      "shippingMethod": "DHL/FedEx/EMS"
    }
  }
}
```

**Поддерживаемые страны:**
- `US` - США
- `EU` - Европа
- `UZ` - Узбекистан
- Другие коды стран

---

## 📊 Проверка результатов

### ✅ Успешная интеграция:
- [ ] Поиск возвращает реальные товары с 1688
- [ ] Информация о товаре содержит все поля
- [ ] Расчет доставки учитывает вес товара
- [ ] Нет ошибок про "mock data"

### ⚠️ Проблемы и решения:

| Проблема | Решение |
|----------|---------|
| `TMAPI_TOKEN is not set` | Добавьте в `.env`: `TMAPI_TOKEN=your_token` |
| `401 Unauthorized` | Проверьте правильность токена |
| Пустые результаты поиска | Попробуйте другое ключевое слово |
| Медленные ответы | Первый запрос медленнее, последующие быстрее |

---

## 🔧 Advanced Testing

### Тест с разными фильтрами

```bash
# Поиск с сортировкой по цене (дешевле)
curl -X POST http://localhost:8080/api/alibaba/search \
  -H "Content-Type: application/json" \
  -d '{
    "keyword": "electronics",
    "pageNo": 1,
    "pageSize": 20,
    "sortBy": "price_asc"
  }'

# Поиск со второй страницы
curl -X POST http://localhost:8080/api/alibaba/search \
  -H "Content-Type: application/json" \
  -d '{
    "keyword": "electronics",
    "pageNo": 2,
    "pageSize": 20
  }'
```

### Тест обработки ошибок

```bash
# Попробуйте с пустым ключевым словом
curl -X POST http://localhost:8080/api/alibaba/search \
  -H "Content-Type: application/json" \
  -d '{"keyword": ""}'
# Должна вернуться ошибка 400

# Попробуйте с несуществующим productId
curl http://localhost:8080/api/alibaba/product/invalid123
# Должна вернуться ошибка 404
```

---

## 🚀 Frontend Testing

### Тестирование в приложении

1. **Откройте Marketplace**
   - Перейдите на страницу `/marketplace`
   - Должны видеть категории и товары

2. **Попробуйте фильтрацию**
   - Выберите категорию
   - Используйте ползунок цены
   - Отсортируйте по рейтингу

3. **Кликните на товар**
   - Должна открыться страница с деталями
   - Должны видеть отзывы и рейтинги

4. **Проверьте валюты**
   - Измените валюту в хедере (USD/CNY/UZS)
   - Цены должны пересчитаться

---

## 📈 Что проверяет этот тест:

✅ **API Connectivity** - работает ли подключение к tmapi.top  
✅ **Authentication** - правильный ли токен  
✅ **Data Transformation** - правильно ли преобразуются данные  
✅ **Error Handling** - правильно ли обрабатываются ошибки  
✅ **Type Safety** - все ли типы корректны  

---

## 📝 Логирование

Если что-то не работает, проверьте логи:

```bash
# В браузере откройте DevTools (F12)
# Console tab - смотрите ошибки
# Network tab - смотрите запросы

# На сервере (в терминале):
npm run dev
# Увидите логи при запросах
```

---

## ✨ Успешный результат

Если всё работает правильно, вы должны видеть:

```
✅ API возвращает реальные товары с 1688
✅ Товары имеют правильные цены и описания
✅ Информация о продавце присутствует
✅ Доставка рассчитывается корректно
✅ Нет никаких mock данных
✅ Приложение работает быстро
```

---

## 🆘 Нужна помощь?

1. Проверьте **TMAPI_SETUP.md** для базовой настройки
2. Читайте **ALIBABA_INTEGRATION.md** для полной документации
3. Посетите https://tmapi.top/docs для API документации
4. Проверьте консоль браузера (F12) для ошибок

**Happy testing! 🎉**
